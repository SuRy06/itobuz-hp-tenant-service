SERVER_PORT=8080
SERVICE_NAME=tenant-mgt-service
LOG_LEVEL=info
MONGODB_URI=""
MONGODB_DB_NAME=""
MONGODB_POOL_SIZE=10
MONGODB_CONNECTION_TIMEOUT=3600
MONGODB_SOCKET_TIMEOUT=""
MONGODB_SERVER_SELECTION_TIMEOUT=""
MONGODB_HEARTBEAT_FREQUENCY=""

# OpenAPI / Swagger Documentation
# Set to 'true' to enable public /openapi.json and /docs endpoints (not recommended for internal services).
# Default: false (only /internal/docs and /internal/openapi.json are available).
# BFF should proxy /internal/openapi.json to serve the OpenAPI spec to UI teams and SDKs.
ENABLE_PUBLIC_OPENAPI=false